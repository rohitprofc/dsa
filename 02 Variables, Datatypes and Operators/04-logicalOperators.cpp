#include <iostream>
using namespace std;

int main() {
    bool x = true, y = false;

    cout << "Logical AND: " << (x && y) << endl;
    cout << "Logical OR: " << (x || y) << endl;
    cout << "Logical NOT: " << (!x) << endl;

    return 0;
}
